{
    "name": "07generadorpasswdtest",
    "version": "1.0.0",
    "scripts": {
        "test": "jest",
        "build": "tsc",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage",
        "test:mutation": "stryker run",
        "docs:clean": "node -e \"require('fs').rmSync('docs',{recursive:true,force:true})\"",
        "docs": "npm run docs:clean && typedoc"
    },
    "devDependencies": {
        "@stryker-mutator/core": "^9.1.1",
        "@stryker-mutator/jest-runner": "^9.1.1",
        "@stryker-mutator/typescript-checker": "^9.1.1",
        "@types/jest": "^30.0.0",
        "@types/node": "^24.6.2",
        "jest": "^30.2.0",
        "ts-jest": "^29.4.4",
        "typedoc": "^0.28.13",
        "typescript": "^5.9.3"
    }
}
